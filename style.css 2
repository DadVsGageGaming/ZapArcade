body {
  margin: 0;
  height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: Arial, sans-serif;
  background: radial-gradient(circle, #1b0033 0%, #000000 100%);
  overflow: hidden;
  position: relative;
}

.intro-container {
  text-align: center;
  z-index: 10;
  position: relative;
}

button {
  margin-top: 20px;
  padding: 12px 24px;
  font-size: 1.5rem;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  background: yellow;
  color: black;
  transition: all 0.3s;
  z-index: 10; /* above lightning */
  position: relative;
}

button:hover {
  transform: scale(1.1);
  background: gold;
}

.lightning {
  position: absolute;
  top: 10%;
  left: 50%;
  width: 5px;
  height: 120px;
  background: yellow;
  box-shadow: 0 0 30px yellow, 0 0 60px gold;
  transform: translateX(-50%);
  z-index: 1; /* behind everything */
  animation: strike 2s infinite;
  opacity: 0;
}

@keyframes strike {
  0%, 80%, 100% { opacity: 0; }
  10%, 12%, 15% { opacity: 1; }
  20%, 22% { opacity: 0; }
}
